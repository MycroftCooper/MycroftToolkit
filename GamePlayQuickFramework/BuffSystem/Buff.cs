namespace BuffSystem {
    public class Buff {
        
    }

    public enum EffectTypes { Positive, Neutral, Negative }
    public enum OverlayRules { AddTime, Overlay, Invalid }
    public enum TriggerRules { ByTimer, ByTicker, OnAdd, OnRemove }
    public enum RemoveRules { ByTimer, ByTicker  }
}